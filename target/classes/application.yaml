spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        ddl-auto: validate

  application:
    name: webstore
  
  datasource:
    url: jdbc:mariadb://localhost:3306/webstore
    username: webstore_user
    password: usersalasana
